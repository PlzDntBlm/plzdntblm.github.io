export class CoreSystem {
    constructor(ui) {
        this.ui = ui;
        this.activeGame = null;
        // Initialization code...
    }

    processSystemCommand(command) {
        // Handle system-level commands
    }

    loadGame(game) {
        // Code to load and initialize games
    }

    // Other system-level methods...
}